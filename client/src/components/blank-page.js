
const { ref,useRoute, onMounted, } = Vue;


const chatPage = {
  setup() {
    const route = useRoute();
   

    onMounted(() => {
     
    });


    return {
    
    };
  },
  template: "<div>Chat Page Placeholder</div>",
};

export default chatPage;
